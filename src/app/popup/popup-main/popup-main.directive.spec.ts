import { PopupMainDirective } from './popup-main.directive';

describe('PopupMainDirective', () => {
  it('should create an instance', () => {
    const directive = new PopupMainDirective();
    expect(directive).toBeTruthy();
  });
});
