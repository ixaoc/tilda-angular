import { ParamsEditorDirective } from './params-editor.directive';

describe('ParamsEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new ParamsEditorDirective();
    expect(directive).toBeTruthy();
  });
});
