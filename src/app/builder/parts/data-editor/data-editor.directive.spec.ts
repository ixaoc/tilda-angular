import { DataEditorDirective } from './data-editor.directive';

describe('DataEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new DataEditorDirective();
    expect(directive).toBeTruthy();
  });
});
